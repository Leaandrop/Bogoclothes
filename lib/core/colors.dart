import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFFFFFFFF);     // Azul Facebook
  static const Color secondary = Color(0xFF242526);   // Fondo gris oscuro
  static const accent = Color(0xFFAA8FFF);  // Lila, úsalo si quieres detalles
  static const card = Colors.white;         // Fondo de tarjetas
  static const danger = Colors.red;

  // Background Colors
  static const Color backgroundLight = Color(0xFF18191A); // Fondo principal
  static const Color backgroundWhite = Color(0xFFFFFFFF); // Cajas y tarjetas

  // Text Colors
  static const Color textPrimary = Color(0xFF00C853);      // Texto blanco
  static const Color textSecondary = Color(0xFFB0B3B8);    // Texto gris claro
  static const Color textBlack = Color(0xFFFFFFFF);        // Solo si usas modo claro

  // Botones / Extras
  static const Color googleButtonBackground = Color(0xFFFFFFFF);
  static const Color googleButtonText = Color(0xFFFFFFFF);
}
